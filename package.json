{
  "name": "solver-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Arkade Market Discovery Protocol v0 registry: solver cards reduced into per-network indexes",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arkade-os/solver-registry.git"
  },
  "bugs": {
    "url": "https://github.com/arkade-os/solver-registry/issues"
  },
  "homepage": "https://arkade-os.github.io/solver-registry/",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "reduce": "node --experimental-strip-types scripts/reduce.ts",
    "release:client": "node scripts/release-discovery-client.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.ts packages/discovery-client/test/*.test.ts",
    "test:client": "node --experimental-strip-types --test packages/discovery-client/test/*.test.ts"
  },
  "dependencies": {
    "@noble/curves": "^1.7.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
